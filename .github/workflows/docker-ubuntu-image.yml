name: Docker Ubuntu 22.04 image

# Keep the original manual workflow entrypoint, but use one publisher for all
# builds so it cannot race the automatic master workflow's rolling image tags.
on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: ./.github/workflows/docker-ubuntu-branch-image.yml
    secrets: inherit
